database:
  type: sqlite
  path: ~/.task-manager/tasks.db
  
  # PostgreSQL configuration (uncomment if using postgres)
  # type: postgres
  # host: localhost
  # port: 5432
  # name: taskmanager
  # user: your_username
  # password: your_password
  # ssl_mode: disable

logging:
  level: info    # debug, info, warn, error
  format: text   # json or text
